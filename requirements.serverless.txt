# AI Body Scan SaaS - Vercel Optimized Dependencies
# ============================================
# Lean dependencies for Vercel serverless deployment
# Removed heavy ML libraries that cause crashes

# Web Framework (FastAPI) - Core only
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6
httpx==0.26.0

# Data Handling
pydantic==2.5.3
pydantic-settings==2.1.0

# Database & Deployment
supabase==2.3.7
postgrest==0.16.2

# Basic utilities
python-dotenv==1.0.0

# Payment Processing (optional - can be lazy loaded)
# paystack-python==0.2.3

# NOTE: For AI body scanning, use client-side MediaPipe or 
# separate ML service. DO NOT include these in serverless:
# - mediapipe (very heavy, causes crashes)
# - tensorflow (too large for serverless)
# - opencv-python-headless (can cause issues)
# - numpy (works but heavy)

# For local development, use full requirements.txt
# For production AI features, consider:
# 1. Using a separate ML API endpoint
# 2. Using Vercel ML (Beta)
# 3. Using a dedicated backend (Render, Railway, etc.)
